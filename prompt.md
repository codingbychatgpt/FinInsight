# 任务目标
1.对于风险评估，在未ai解析之前，状态一律标为“未评估”
2.把页面里所有的英文全部变成中文，已有中文的地方，英文翻译直接删掉避免重复，这个网页只给中国人看
3.每次从详情页回到主页的时候，默认刷新一下网页，这样能更新最新的风险评估等主页的数据
4.详情页返回按钮需要固定在页面左上角，滚动页面时也一直可见；实现时保留详情页进入/离场动画，推荐把返回按钮放在不带 transform 的根层，把内容区域包进单独的动画层。
